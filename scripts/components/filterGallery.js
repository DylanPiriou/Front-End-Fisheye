// const dropdown = document.querySelector('.dropdown');
// const buttons = document.querySelectorAll('.dropdown-content button');

// dropdown.addEventListener('click', () => {
//   dropdown.classList.toggle('active');
// });

// buttons.forEach(button => {
//   button.addEventListener('click', () => {
//     const selectedValue = button.getAttribute('data-value');
//     const currentBtn = document.querySelector('.dropbtn');
//     const currentValue = currentBtn.getAttribute('data-value');
    
//     // Échange les attributs data-value
//     currentBtn.setAttribute('data-value', selectedValue);
//     button.setAttribute('data-value', currentValue);

//     // Échange les textes des boutons
//     const currentText = currentBtn.textContent.trim();
//     currentBtn.textContent = button.textContent.trim();
//     button.textContent = currentText;
//   });
// });
